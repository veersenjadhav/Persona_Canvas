---
description: Step-by-step workflow to install dependencies, run typecheck, and build the Persona Canvas Next.js project.
---

# Build Project Workflow

Follow these steps to install dependencies, typecheck, and compile the application:

1. **Ensure Node.js and npm are on PATH**
   - Node.js (>= 20) is required.

2. **Install Dependencies**
   ```bash
   npm install
   ```

3. **Run TypeScript Type Check**
   ```bash
   npm run typecheck
   ```

4. **Compile Production Build**
   ```bash
   npm run build
   ```
